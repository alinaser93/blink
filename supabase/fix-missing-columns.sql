-- =====================================================================
--  إصلاح سريع لقاعدة Supabase قديمة تنقصها أعمدة (آمن لإعادة التشغيل)
--  العَرَض: عند الاستيراد يظهر "column categories.sort_order does not exist"،
--  أو لا تظهر الأقسام في اللوحة رغم الاتصال بالقاعدة.
--  التشغيل: Supabase → SQL Editor → New query → الصق → Run، ثم أعد الاستيراد.
-- =====================================================================

-- ترتيب الأقسام داخل المجموعة (يحتاجه إنشاء/ترتيب الأقسام والتفرّعات)
alter table public.categories add column if not exists sort_order integer not null default 0;

-- السعر قبل الخصم للمنتجات (اختياري؛ NULL/0 = لا خصم)
alter table public.products add column if not exists mrp_iqd integer;

-- =====================================================================
--  تمّ ✅ بعد التشغيل أعد «استيراد» الملف من لوحة الأدمن.
--  (هذا جزء من schema.sql الكامل — تشغيل schema.sql كاملاً يؤدي نفس الغرض.)
-- =====================================================================
